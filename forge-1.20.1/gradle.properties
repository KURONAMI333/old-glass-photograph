# 1.20.1 Forge cell — independent Gradle build (own wrapper / settings.gradle).
# The repo root's settings.gradle does not include this directory, so the existing
# cells are structurally untouched by this cell.
# Build stack follows mod-076/forge-1.20.1 (LOADERS.md: ForgeGradle 6.0.16 / Gradle 8.10.2 / Java 17).

org.gradle.jvmargs=-Xmx3G
org.gradle.daemon=false
org.gradle.caching=true

version=0.1.2
group=com.kuronami.oldglassphotograph

minecraft_version=1.20.1
minecraft_version_range=[1.20.1,1.21)
forge_version=47.4.13
forge_version_range=[47,)
loader_version_range=[47,)

parchment_minecraft_version=1.20.1
parchment_mappings_version=2023.09.03

mod_name=Old Glass Photograph
mod_author=KURONAMI
mod_id=old_glass_photograph
mod_license=All Rights Reserved
description=19th-century wet plate photography. Coat the glass, sensitize it, shoot before it dries, and develop it in the darkroom. Anything that moves during the exposure vanishes from the plate.

# Jade tooltip (compileOnly soft-dep via fg.deobf). version id xJQHCmWJ = 11.13.3+forge,
# mechanically extracted from the Modrinth API response saved by contract #38
# (workspace/oxalpha-runs/ogp-unverified-2026-08-26/evidence/jade_versions_modrinth.json).
jade_version_id=xJQHCmWJ

# Gradle daemon runs on JDK 21; toolchain below compiles for Java 17 (LOADERS.md).
# Shipped classes must be major 61.
org.gradle.java.home=C:/Program Files/Eclipse Adoptium/jdk-21.0.10.7-hotspot
